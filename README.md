# LLM Status Monitor

Одностраничное веб-приложение для мониторинга статуса доступности LLM моделей в реальном времени.

## Возможности

- ✅ Мониторинг статуса доступности моделей
- ✅ Отображение времени ответа (latency)
- ✅ Автоматическое обновление каждые 30 секунд
- ✅ Ручное обновление по кнопке
- ✅ Группировка моделей по провайдерам
- ✅ Современный адаптивный дизайн
- ✅ Обработка ошибок и CORS
- ✅ Безопасное хранение конфигурации в localStorage браузера
- ✅ Форма для ввода конфигурации (секреты не попадают в репозиторий)

## Структура проекта

```
sbt-immers-mon/
├── index.html              # Основная HTML разметка
├── style.css               # Стили приложения
├── script.js               # Логика проверки статуса
├── config.example.json     # Пример конфигурации (без секретов)
├── netlify.toml            # Конфигурация Netlify
├── netlify/
│   └── functions/
│       └── proxy.js        # Netlify функция-прокси для обхода CORS
└── README.md               # Документация
```

**Важно:** Файл `config.json` с реальными токенами не должен попадать в репозиторий. Он добавлен в `.gitignore` для безопасности.

## Развертывание на Netlify (рекомендуется)

Netlify поддерживает serverless functions, что позволяет обойти проблему CORS через прокси-сервер.

### Преимущества Netlify:
- ✅ Решает проблему CORS через прокси-функцию
- ✅ Автоматический деплой из GitHub
- ✅ Бесплатный тарифный план
- ✅ Быстрое развертывание

### Шаг 1: Подготовка репозитория

Убедитесь, что все файлы загружены в GitHub репозиторий:
```bash
git add .
git commit -m "Add Netlify support with proxy function"
git push origin main
```

### Шаг 2: Создание аккаунта на Netlify

1. Перейдите на [netlify.com](https://www.netlify.com)
2. Зарегистрируйтесь или войдите через GitHub

### Шаг 3: Подключение репозитория

1. В Dashboard Netlify нажмите "Add new site" → "Import an existing project"
2. Выберите "GitHub" и авторизуйтесь
3. Выберите репозиторий `llm-status-monitor`
4. Настройки сборки:
   - **Build command:** (оставьте пустым, сборка не требуется)
   - **Publish directory:** `.` (корневая директория)
5. Нажмите "Deploy site"

### Шаг 4: Настройка функций

Netlify автоматически обнаружит функции в папке `netlify/functions/`. Прокси-функция будет доступна по адресу:
```
https://your-site.netlify.app/.netlify/functions/proxy
```

### Шаг 5: Доступ к приложению

Через 1-2 минуты приложение будет доступно по адресу:
```
https://your-site-name.netlify.app
```

### Как работает прокси:

1. Браузер делает запрос к `/.netlify/functions/proxy` (тот же домен)
2. Netlify функция получает запрос и проксирует его к API моделей
3. Ответ возвращается браузеру
4. CORS не блокирует, так как запрос идет на тот же домен

### Обновление сайта

При каждом push в ветку `main` Netlify автоматически пересоберет и задеплоит сайт.

## Развертывание на GitHub Pages

**Примечание:** На GitHub Pages могут быть проблемы с CORS, если серверы моделей не настроены для разрешения кросс-доменных запросов. Для решения этой проблемы рекомендуется использовать Netlify (см. раздел выше).

### Шаг 1: Создание репозитория

1. Создайте новый репозиторий на GitHub
2. Назовите его, например, `llm-status-monitor`

### Шаг 2: Загрузка файлов

1. Клонируйте репозиторий локально:
   ```bash
   git clone https://github.com/your-username/llm-status-monitor.git
   cd llm-status-monitor
   ```

2. Скопируйте все файлы проекта в репозиторий:
   - `index.html`
   - `style.css`
   - `script.js`
   - `config.example.json`
   - `README.md`
   - `.gitignore`

3. Закоммитьте и запушьте файлы:
   ```bash
   git add .
   git commit -m "Initial commit: LLM Status Monitor"
   git push origin main
   ```

### Шаг 3: Включение GitHub Pages

1. Перейдите в настройки репозитория (Settings)
2. В левом меню выберите "Pages"
3. В разделе "Source" выберите:
   - Branch: `main` (или `master`)
   - Folder: `/ (root)`
4. Нажмите "Save"

### Шаг 4: Доступ к приложению

Через несколько минут ваше приложение будет доступно по адресу:
```
https://your-username.github.io/llm-status-monitor/
```

## Настройка конфигурации

### Безопасное хранение конфигурации

**Важно:** Конфигурация с токенами хранится только в localStorage браузера и никогда не передается на сервер. Это позволяет использовать публичный репозиторий без риска утечки секретов.

### Первая настройка

При первом открытии приложения вы увидите форму для настройки конфигурации. Есть два способа:

#### Вариант 1: Загрузка из файла (рекомендуется)

1. Скачайте файл `config.example.json` из репозитория
2. Скопируйте его как `config.json`
3. Заполните реальными данными (URL, токены)
4. В приложении нажмите "⚙️ Настройки" → "Выбрать файл config.json"
5. Конфигурация сохранится в localStorage браузера

#### Вариант 2: Ввод вручную

1. В приложении нажмите "⚙️ Настройки"
2. Вставьте JSON конфигурацию в текстовое поле
3. Нажмите "💾 Сохранить конфигурацию"

### Формат конфигурации

```json
{
  "ProviderName": {
    "ModelName": {
      "url": "http://example.com:8000",
      "model": "/model/path",
      "access_token": "your-token"
    }
  }
}
```

### Изменение конфигурации

В любой момент можно изменить конфигурацию, нажав кнопку "⚙️ Настройки" в шапке приложения.

**Примечание:** Конфигурация хранится локально в браузере. При очистке данных браузера потребуется повторная настройка.

## Важные замечания

### CORS (Cross-Origin Resource Sharing)

Если API серверы моделей не разрешают CORS запросы из браузера, вы увидите предупреждение на странице. В этом случае есть несколько вариантов:

1. **Настроить CORS на стороне API серверов** (рекомендуется)
   - Добавить заголовки `Access-Control-Allow-Origin` на серверах моделей

2. **Использовать прокси-сервер**
   - Развернуть простой Node.js прокси (см. раздел ниже)
   - Или использовать Netlify/Vercel с serverless functions

3. **Локальный прокси для разработки**
   - Использовать расширение браузера для обхода CORS (только для разработки)

### Локальный прокси-сервер (опционально)

Если нужен прокси для обхода CORS, создайте файл `server.js`:

```javascript
const express = require('express');
const cors = require('cors');
const { createProxyMiddleware } = require('http-proxy-middleware');

const app = express();
app.use(cors());
app.use(express.static('.'));

// Прокси для API запросов
app.use('/api/proxy', createProxyMiddleware({
    target: 'http://target-url',
    changeOrigin: true,
    pathRewrite: {
        '^/api/proxy': ''
    }
}));

const PORT = process.env.PORT || 3000;
app.listen(PORT, () => {
    console.log(`Server running on http://localhost:${PORT}`);
});
```

Запуск:
```bash
npm install express cors http-proxy-middleware
node server.js
```

## Технологии

- HTML5
- Vanilla JavaScript (ES6+)
- CSS3 (Flexbox, Grid)
- OpenAI-compatible API

## Лицензия

MIT

